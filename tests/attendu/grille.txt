[v3] HC_V3=absent (défaut 1) -> exécuteur v3 ACTIF
=== 1. elle se LIT ===
   put the grid
   false
=== 2. elle s'ÉCRIT, et la lecture suit ===
   set the grid to true
   [HOTE] grid <- « true » donc vrai
   put the grid
   true
   set the grid to false
   [HOTE] grid <- « false » donc faux
   put the grid
   false
=== 3. l'aller-retour dans un même script ===
   (une pile qui cale un réglage et le rend : l'usage réel)
   put the grid into avant
  set the grid to true
  put "pendant : " & the grid
  set the grid to avant
  put "après : " & the grid
   [HOTE] grid <- « true » donc vrai
   pendant : true
   [HOTE] grid <- « false » donc faux
   après : false
=== 4. on ne l'a PAS rendue écrivable au prix d'une porte ouverte ===
   (une coquille doit toujours se plaindre — c'est tout l'objet
    de setmuet.c, et une liste élargie pourrait l'avoir défait)
   set the gird to true
   [ERR]    !! propriété inconnue : gird
   put the gird
   [ERR]    !! propriété ou fonction inconnue : gird (v3, ligne 2 de stack "Grille".essai)
   set the screenRect to "0,0,1,1"
   [ERR]    !! propriété en lecture seule : screenRect
=== 5. si l'hôte ne répond pas, on ne se tait pas : on le dit ===
   put the grid
   [ERR]    !! propriété ou fonction inconnue : grid (v3, ligne 2 de stack "Grille".essai)
=== 6. le témoin : une propriété voisine n'a pas bougé ===
   put the filled
   false
=== 7. « the polySides », l'autre réglage posé au même endroit ===
   (ajoutée aux deux MÊMES listes ; si l'une avait été oubliée,
    elle se lirait sans s'écrire, ou l'inverse)
   put the polySides
   4
   set the polySides to 6
   [HOTE] polySides <- « 6 » donc 6
   put the polySides
   6
=== 8. hors domaine : l'hôte ramène dans les bornes ===
   (un polygone à deux côtés n'enferme rien, et à mille il n'est
    plus qu'un cercle — mais c'est l'hôte qui tranche, pas le
    noyau : il ne connaît pas le sens de cette propriété)
   set the polySides to 2
   [HOTE] polySides <- « 2 » donc 3
   put the polySides
   3
   set the polySides to 9999
   [HOTE] polySides <- « 9999 » donc 50
   put the polySides
   50
=== 9. et la coquille reste refusée ===
   set the polySide to 6
   [ERR]    !! propriété inconnue : polySide
=== 10. « the transparent », le voisin de « the filled » ===
   (les deux sont des cases VOISINES de la palette d'outils, et
    un seul des deux se posait — le motif de toujours)
   put the filled & " / " & the transparent
   false / false
   set the transparent to true
   [HOTE] transparent <- « true » donc vrai
   put the transparent
   true
   set the transparent to false
   [HOTE] transparent <- « false » donc faux
   put the transparent
   false
=== 11. « the centered », signalée par une pile réelle ===
   (une pile de traceur de courbes la pose à chaque idle ; sans
    elle, le gestionnaire tombait en erreur à chaque battement)
   put the centered
   false
   set the centered to true
   [HOTE] centered <- « true » donc vrai
   put the centered
   true
   set the centered to false
   [HOTE] centered <- « false » donc faux
   put the centered
   false
=== 12. la ligne EXACTE de la pile signalée ===
   (« if not the centered then set the centered to false » : la
    propriété est lue par « not », donc elle doit rendre un
    booléen que le noyau sait nier, pas une chaîne quelconque)
   if not the centered then set the centered to false
  put "idle survécu, centered = " & the centered
   [HOTE] centered <- « false » donc faux
   idle survécu, centered = false
   set the centered to true
  if not the centered then set the centered to false
  put "restée : " & the centered
   [HOTE] centered <- « true » donc vrai
   restée : true
=== 13. et la coquille reste refusée ===
   set the centred to true
   [ERR]    !! propriété inconnue : centred
   put the centred
   [ERR]    !! propriété ou fonction inconnue : centred (v3, ligne 2 de stack "Grille".essai)
