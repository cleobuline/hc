[v3] HC_V3=absent (défaut 1) -> exécuteur v3 ACTIF
=== 1. les trois cas de base ===
   INF
   -INF
   NAN(004)
=== 2. LE CAS DE LA PILE : l'équation, au point qui cassait ===
   (x = -32/64, soit -0.5 exactement, avec son numberFormat)
   -0.500,NAN(004)
=== 3. et le point d'à côté, qui marchait déjà ===
   (x = -31/64 : elle lit « -0.484,1.188 » à l'écran, et c'est
    bien 1.188 et non 1.194 — le numberFormat arrondit AUSSI les
    calculs intermédiaires, comme chez HyperCard)
   -0.484,1.188
=== 4. le CODE survit aux calculs qui suivent ===
   (dans l'équation, le 0/0 est suivi d'une division par cinq :
    si le code ne voyageait pas, on lirait « NAN » tout court)
   z      = NAN(004)
   z/5    = NAN(004)
   z*2+1  = NAN(004)
=== 5. « divide by 0 », le JUMEAU : la commande, pas l'opérateur ===
   (une pile peut employer l'une ou l'autre ; corriger la seule
    expression aurait laissé la moitié du défaut en place)
   7 divisé par 0 : INF
   0 divisé par 0 : NAN(004)
=== 6. INF s'ORDONNE : c'est un nombre ===
   (« if ny > itt » de la pile doit trancher sur des nombres)
   y is INF      : true
   y > 625       : true
   -1/0 < -625   : true
   INF + 1       : INF
=== 7. NAN se CALCULE, mais ne s'ORDONNE pas ===
   (les deux comparaisons que la pile écrit doivent répondre vrai,
    et « y = 5 » doit répondre FAUX : un NaN n'est égal à rien
    d'autre qu'à un NaN du même code)
   y contains NAN   : true
   y is NAN(004)    : true
   y is NAN(037)    : false
   y = 5            : false
   y is not 5       : true
=== 7b. L'ORDRE se fait sur le TEXTE, et c'est la pile qui tranche ===
   (j'avais d'abord fait rendre false aux quatre, au nom de la
    virgule flottante. C'était raisonner sur IEEE au lieu de
    regarder la pile — voir 7c, qui est la mesure qui compte)
   y > 625   : true
   y < 625   : false
   y = 5     : false
=== 7b-bis. L'ORDRE NE DÉPEND PAS DE L'ÉCRITURE DU NaN ===
   (RÉGRESSION VÉCUE, une demi-heure après avoir ajouté le signe.
    La première règle se repliait sur la comparaison de TEXTE, et
    marchait par accident d'alphabet : « NAN(004) » > « 625 »
    parce que « N » vient après « 6 ». Le moins unaire retourne le
    bit de signe d'un NaN, si bien que la chaîne de la pile —
      round(-(y-cy)*yScale + 171)
    rendait « -NAN(004) », qui compare AVANT « 625 ». Le garde a
    cessé de partir et le traceur est reparti dessiner vers un
    point non fini.

    La règle est maintenant ÉNONCÉE : un NaN se range APRÈS tout
    nombre, quel que soit son code et quel que soit son signe)
   NAN(004)  > 625 : true
   -NAN(004) > 625 : true
   -NAN(001) > 625 : true
   NAN(036)  > 625 : true
   et dans l'autre sens, tous faux :
   NAN(004)  < 625 : false
   -NAN(004) < 625 : false
=== 7b-ter. LA CHAÎNE EXACTE DE LA PILE, bout à bout ===
   (c'est la ligne qui a cassé : le calcul, puis le garde)
   ny vaut -NAN(004)
   le garde part : crayon levé
=== 7c. LE GARDE DE LA PILE, tel qu'il est écrit ===
   (if ny > itt or ny < -itt or nx > itt or nx < -itt then
      if y contains "NAN" or y is "INF" then …
      put empty into nx        -- on lève le crayon

    Le traitement du NaN est DANS le test de bornes : pour être
    seulement atteignable, « ny > itt » doit répondre VRAI sur un
    NaN. Avec l'ordre à false le garde ne partait pas, la pile
    traçait vers un point non fini, et la courbe recevait une
    barre verticale en travers. Mesuré à l'écran)
   ny vaut -NAN(004)
   ERR: x=-0.5,y=NAN(004)
   y=0/0 (indeterminate)
   crayon levé : la courbe aura un TROU, pas une barre
=== 7d. et si une pile y va quand même : on REFUSE de dessiner ===
   (coord_champ passe par hc_coord, qui rend le DÉFAUT — zéro —
    quand il ne sait pas lire. Zéro est une coordonnée valide :
    rien ne distinguait le bord de la carte d'une valeur qu'on
    n'avait pas su lire, et c'est ce qui rendait la barre muette)
   [ERR]    !! drag : « NAN(004) » n'est pas une coordonnée
   [ERR]    !! click : « NAN(004) » n'est pas une coordonnée
   et le témoin, lui, passe :
   [ERR]    !! drag : l'hôte ne gère pas la souris
=== 7e. LES TROIS CODES MESURÉS SOUS HYPERCARD ===
   (relevés dans la boîte de message de HyperCard sous Basilisk II,
    pas déduits d'une table — je m'étais trompé en pensant 022 pour
    le logarithme. Le C ne les donne pas : sur cette machine les
    trois rendent un NaN de charge NULLE, il faut les poser)
   ln(-1)    = NAN(036)
   sqrt(-1)  = -NAN(001)
   0*(1/0)   = NAN(008)
=== 7f. le SIGNE compte : sqrt(-1) porte un moins, pas les autres ===
   (le bit de signe d'un NaN n'a aucun sens arithmétique, mais SANE
    le montre et une pile peut le comparer)
   y is -NAN(001) : true
   y is NAN(001)  : false
   y contains NAN : true
=== 7g. LA CHARGE SE LIT EN DÉCIMAL, PAS EN OCTAL ===
   (strtod lit la charge d'un NaN en base 0 : un zéro de tête la
    fait passer en OCTAL, et quatre des six codes que HyperCard
    écrit se corrompaient en silence —
      NAN(008) -> 0    NAN(009) -> 0
      NAN(036) -> 30   NAN(037) -> 31
    NAN(037) est écrit EN DUR dans la pile, et le code voyage dans
    la charge : la corruption aurait survécu à tous les calculs)
   NAN(001) relu : NAN(001)
   NAN(008) relu : NAN(008)
   NAN(009) relu : NAN(009)
   NAN(036) relu : NAN(036)
   NAN(037) relu : NAN(037)
   -NAN(001) relu : -NAN(001)
=== 7h. et le code survit aux calculs, comme celui de 0/0 ===
   ln(-1)/5   = NAN(036)
   sqrt(-1)*2 = -NAN(001)
=== 7i. le témoin : les domaines VALIDES n'ont pas bougé ===
   ln(1)     = 0
   ln(0)     = -INF
   sqrt(4)   = 2
   0*5       = 0
   2*(1/0)   = INF
=== 8. « mod » par zéro est une ERREUR, pas une valeur ===
   (MESURÉ : « put 5 mod 0 » sous HyperCard ouvre un dialogue,
    « can't mod by 0 ». J'avais supposé qu'il rendait un NaN comme
    la division, faute d'en connaître le code SANE — faux. Les deux
    opérations se ressemblent et HyperCard ne les traite pas pareil,
    ce qui est précisément pourquoi on mesure au lieu de déduire)
   [ERR]    !! modulo par zéro (v3, ligne 3 de stack "SANE".essai)
   1
=== 9. « div » par zéro REFUSE aussi ===
   (MESURÉ : « put 7 div 0 » ET « put 0 div 0 » ouvrent tous deux
    le dialogue « can't div by zero ». J'avais fait suivre à div la
    règle de « / » parce que c'est une division — deuxième fois que
    la déduction se fait démentir sur la même famille.

    La cohérence est lisible une fois qu'on la voit : div et mod
    sont des opérations ENTIÈRES, et l'arithmétique SANE qui
    fabrique INF et les NaN est celle des FLOTTANTS. Seule « / »
    y a droit)
   [ERR]    !! division entière par zéro (v3, ligne 2 de stack "SANE".essai)
   [ERR]    !! division entière par zéro (v3, ligne 2 de stack "SANE".essai)
   3
=== 9b. « divide … by 0 », la commande : une VALEUR ===
   (MESURÉ, et il le fallait, puisque les deux opérations d'à côté
    font l'inverse. La ligne de partage ne passe donc pas entre
    l'expression et la commande, mais entre le FLOTTANT et
    l'ENTIER : « / » et « divide » sont la même division flottante
    sous deux écritures, « div » et « mod » sont entières et
    refusent.

    Le cas 0/0 de la commande n'a pas été relevé à part et n'a pas
    à l'être : il emprunte la même ligne de code que l'opérateur,
    les deux ne peuvent pas diverger)
   7 divisé par 0 : INF
   0 divisé par 0 : NAN(004)
=== 10. les témoins : ce qui marchait marche encore ===
   6/3     = 2
   7 div 2 = 3
   7 mod 2 = 1
   1e308*2 = INF
   1.5e308 = 1.5e+308
