[v3] HC_V3=absent (défaut 1) -> exécuteur v3 ACTIF
── témoin (doit dire TROU)
   servi  the zorglub                -> (vide)
   servi  the blurp                  -> (vide)

── ce qu'HyperCard offrait
   servi  the date                   -> 09/11/25
   servi  the time                   -> 16:00
   servi  the seconds                -> 3840451200
   hote   the ticks                  -> <hote>
   servi  the result                 -> (vide)
   servi  the selection              -> (vide)
   servi  the selectedText           -> (vide)
   servi  the selectedChunk          -> (vide)
   servi  the selectedField          -> (vide)
   servi  the selectedLine           -> (vide)
   hote   the clickText              -> <hote>
   hote   the clickLoc               -> <hote>
   hote   the clickChunk             -> <hote>
   hote   the mouse                  -> <hote>
   hote   the mouseLoc               -> <hote>
   hote   the mouseClick             -> <hote>
   hote   the shiftKey               -> <hote>
   hote   the optionKey              -> <hote>
   hote   the commandKey             -> <hote>
   hote   the tool                   -> <hote>
   hote   the screenRect             -> <hote>
   servi  the target                 -> B
   servi  the paramCount             -> 0
   servi  the params                 -> t
   servi  the itemDelimiter          -> ,
   servi  the number of cards        -> 1
   hote   the foreColor              -> <hote>
   hote   the pattern                -> <hote>
   hote   the brush                  -> <hote>
   hote   the lineSize               -> <hote>
   hote   the textFont               -> <hote>
   hote   the textSize               -> <hote>
   servi  the stacks                 -> (vide)
   servi  the windows                -> (vide)
   servi  the programs               -> (vide)
   servi  the sound                  -> (vide)
   servi  the menus                  -> (vide)
   servi  the diskSpace              -> (vide)
   servi  the heapSpace              -> (vide)
   servi  the systemVersion          -> (vide)
   servi  the version                -> (vide)
   servi  the destination            -> (vide)
   servi  the size                   -> (vide)
   servi  the freeSize               -> (vide)
   servi  the language               -> (vide)
   servi  the cursor                 -> (vide)
   servi  the numberFormat           -> (vide)
   servi  the userLevel              -> 5
   servi  the blindTyping            -> false
   servi  the powerKeys              -> false
   servi  the dragSpeed              -> 0
   servi  the lockRecent             -> false
   servi  the lockScreen             -> false
   servi  the lockMessages           -> false
   servi  the editBkgnd              -> (vide)
   servi  the scriptTextSize         -> (vide)
   servi  the scriptTextFont         -> (vide)
   servi  the suspended              -> (vide)
   servi  the messageWatcher         -> (vide)
   servi  the variableWatcher        -> (vide)
   servi  the printMargins           -> (vide)
   servi  the printTextFont          -> (vide)
   servi  the idleRate               -> (vide)
   servi  the environment            -> (vide)
   servi  the address                -> (vide)
   servi  the longWindowTitles       -> (vide)
   servi  the userModify             -> (vide)
   servi  the debugger               -> (vide)
